#!/usr/bin/env bash
# Configures localhost and facebook.com IPs in /etc/hosts

set -e

HOSTS_FILE="/etc/hosts"

# Remove existing localhost and facebook.com lines
sudo sed -i '/[[:space:]]localhost$/d' "$HOSTS_FILE"
sudo sed -i '/[[:space:]]facebook\.com$/d' "$HOSTS_FILE"

# Add new entries
echo "127.0.0.2 localhost" | sudo tee -a "$HOSTS_FILE" > /dev/null
echo "8.8.8.8 facebook.com" | sudo tee -a "$HOSTS_FILE" > /dev/null
